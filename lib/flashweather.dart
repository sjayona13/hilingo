import 'package:flutter/material.dart';

class FlashWeather extends StatelessWidget {
  const FlashWeather
  ({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Greetings')),
      body: const Center(child: Text('This is the Greetings paasdasdasdasdge')),
    );
  }
}
